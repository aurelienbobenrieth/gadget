---
title: "model-action-no-transactional-timeout-mismatch"
description: "Disallow timeoutMS above 5000ms on model actions without explicitly setting transactional: false."
keywords: ["eslint", "gadget", "model-action-no-transactional-timeout-mismatch"]
---

This page is generated from the rule metadata and tests.

- Scope: Model action files
- Type: `problem`
- Fixable: no
- Recommended: `warn`
- Strict: `error`

## Description

Disallow timeoutMS above 5000ms on model actions without explicitly setting transactional: false.

## Reported messages

- `mismatch`: timeoutMS {{ value }}ms has no effect on transactional model actions above 5000ms.

## Invalid example

```ts
export const options: ActionOptions = { timeoutMS: 6000 };
```

## Valid example

```ts
export const options: ActionOptions = { timeoutMS: 3000 };
```
